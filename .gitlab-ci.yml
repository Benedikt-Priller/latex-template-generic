pages:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying static site from public/"
  pages:
    publish: public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

# Comment